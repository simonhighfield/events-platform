import { supabase } from "./supabaseClient";

export default async function deleteSavedEventById(savedEventId) {
    // console.log('in delete: ', savedEventId)
    
//   const { data, error } = await supabase
//     .from('saved_events')
//     .delete()
//     .eq('id', savedEventId)
//     .select();
//   if (data) {
//     return { deletedEvent : data[0]}
//   }
//   if (error) {
//     throw {error}
//   }
} 